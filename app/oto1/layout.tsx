import type {Metadata} from 'next';
export const metadata:Metadata={title:'The Household Continuity Plan | The ICE Vault',description:'Give your family a personalised plan for keeping the household running for the next 30 days.'};
export default function Layout({children}:{children:React.ReactNode}){return children;}
